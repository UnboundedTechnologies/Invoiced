import { ComingSoon } from "@/components/coming-soon";
export default function Page() {
  return (
    <ComingSoon
      title="Paycheques (T4)"
      phase="Phase 2"
      gated="Salary tool stays disabled until you register a CRA payroll program account (RP0001) for Unbounded Technologies Inc. and flip the switch in Settings. Until then, use Dividends (T5)."
    />
  );
}
